package uk.co.jordandick.tesco.vending.model;

/**
 * The Enum State.
 */
public enum State {

                   /** The off state. */
                   OFF_STATE,
                   /** The on state. */
                   ON_STATE,
                   /** The has balance. */
                   HAS_BALANCE,
                   /** The has no balance. */
                   HAS_NO_BALANCE,

                   /** The product sold. */
                   PRODUCT_SOLD;
}
